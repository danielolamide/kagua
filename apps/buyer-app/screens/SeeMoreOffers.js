import { SafeAreaView } from "react-native";
import { Text, GoBack } from "@kagua/ui";

export default function SeeMoreOffers() {
	return (
		<SafeAreaView>
			<GoBack />
			<Text>See More</Text>
		</SafeAreaView>
	);
}
